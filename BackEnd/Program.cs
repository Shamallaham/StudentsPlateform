using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Data.Entity;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace TCC.BackEnd
{
    public class Program
    {
        public static void Main(string[] args)
        {
            //User u1 = new User();
            //u1.Name = "saker";
            //u1.Email = "saker@admin.com";
            //u1.Password = "sezardnew123";
            //u1.AcademicYear = 2;
            //u1.Father = "mahmod";
            //u1.Mother = "najod";
            //u1.Nickname = "dakak";
            //u1.Phone = "0934030517";
            //u1.Specialization = "programming";
            //db.Users.Add(u1);
            //db.SaveChanges();


            //TccDB db = new TccDB();
            //db.Database.Delete();
            //db.Database.CreateIfNotExists();


            //TccDB db = new TccDB();
            //IEnumerable<User> users = db.Users.Where(u => u.Name=="saker");
            ////IEnumerable<User> users = db.Users.Where(u => u.Name.Contains("s"));
            //foreach (User item in users)
            //{
            //    Console.WriteLine(item.Name + "\t" + item.Nickname);
            //}

        }
    }
    
}
